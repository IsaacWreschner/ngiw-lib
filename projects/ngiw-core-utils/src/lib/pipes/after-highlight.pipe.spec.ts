import { AfterHighlightPipe } from './after-highlight.pipe';

describe('AfterHighlightPipe', () => {
  it('create an instance', () => {
    const pipe = new AfterHighlightPipe();
    expect(pipe).toBeTruthy();
  });
});
