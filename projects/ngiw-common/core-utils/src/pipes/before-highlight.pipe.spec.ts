import { BeforeHighlightPipe } from './before-highlight.pipe';

describe('BeforeHighlightPipe', () => {
  it('create an instance', () => {
    const pipe = new BeforeHighlightPipe();
    expect(pipe).toBeTruthy();
  });
});
